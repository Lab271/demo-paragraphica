# Issue drafts (repo was archived at time of writing — create after unarchive or in new repo)

Each is `gh issue create --label enhancement --title "<title>" --body-file <body>`.

## Phase 1: core pipeline, cloud backend (GPT Image 2), Typer CLI
Unblocks all other phases.
- Context dataclass wrapping address, weather, local time
- Prompt from a template, testable without network
- Backend protocol: describe(), image()
- cloud backend on OpenAI: GPT Image 2 replaces DALL-E 3; structured-output description + one image call
- Timezone offline via timezonefinder + zoneinfo; drop geotimezone.com
- Remove mapbox SDK from terra_virtualis.py (api.py already uses requests)
- Typer CLI: `terra generate --location --style --context`, `--dry-run` prints description + prompt only
- Reuse recorded fixtures in tests/

## Phase 2: packaging with uv, lockfile, Ansible via uv sync
- Full pyproject.toml, [project.scripts], keep ruff config
- uv.lock committed; remove requirements.txt and setup.py
- Makefile dev = uv sync; drop pip lines and .para target
- Ansible: checkout + uv sync replaces per-file copies; keep 1Password lookups
- README install section

## Phase 3: persist every generation
- output/<timestamp>-<slug>.png + output/history.jsonl (location, lat/lon, style, context, description, prompt, revised prompt, backend, model, duration)
- `terra history`
- Static gallery page from the JSONL

## Phase 4: local backend on Mac Mini (Ollama + Draw Things/mflux) and benchmark
- Check Mini RAM first (16 GB floor, 24 GB+ comfortable)
- Ollama small model for description
- Draw Things HTTP API for image; mflux fallback
- Benchmark Flux schnell vs dev at 1024 px on the existing styles, esp. lego / pop-up
- Config switch cloud|local; cloud stays as fallback
- Decision gate for phases 6/7

## Phase 5: FastAPI service exposing the core
- POST /generate, GET /history, GET /styles, GET /contexts
- launchd/systemd unit on the Mini
- API keys never leave the Mini

## Phase 6: Pi device UI — pygame/framebuffer + GPIO, remove Streamlit + Chromium
- Viewer: image, then description/prompt on demand
- Rotary encoder for style, button for generate (see tests/test_rotary.py)
- GPS HAT or fixed location
- Thin client to the Mini service
- Remove terra_virtualis.py, streamlit, start-kiosk.sh, gpt-viewer.service
- systemd unit for the viewer

## Phase 7 (optional): Rust Pi client, single cross-compiled binary
- rppal GPIO, framebuffer, one POST, render PNG; ~300 lines
- Only after phase 6 and a stable HTTP contract; wontfix if pygame suffices
